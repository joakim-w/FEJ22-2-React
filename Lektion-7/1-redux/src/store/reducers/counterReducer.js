const initState = {
  count: 0
}

// {
//   type: 'INCREMENT',
//   payload: 1
// }

const counterReducer = (state = initState, action) => {
  switch(action.type) {

    default:
      return state
  }
}

export default counterReducer;